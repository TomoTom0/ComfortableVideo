{
  "name": "comfort-movie",
  "version": "1.0.0",
  "description": "Comfort Movie - 快適な動画視聴環境を提供するChrome拡張機能",
  "scripts": {
    "build": "tsc && npm run copy-assets",
    "copy-assets": "cp manifest.json dist/ && cp src/*.css dist/ 2>/dev/null || true && cp src/*.html dist/ 2>/dev/null || true && cp -r icons dist/ 2>/dev/null || true && cp -r _locales dist/ 2>/dev/null || true",
    "watch": "tsc --watch",
    "clean": "rm -rf dist",
    "rebuild": "npm run clean && npm run build",
    "build:script": "./scripts/build.sh",
    "deploy": "./scripts/rsync.sh",
    "package": "./scripts/package.sh",
    "build-and-deploy": "./scripts/build-and-deploy.sh"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/chrome": "^0.0.246"
  },
  "keywords": [
    "chrome-extension",
    "video",
    "comfortable-mode",
    "typescript"
  ],
  "author": "Your Name",
  "license": "MIT"
}